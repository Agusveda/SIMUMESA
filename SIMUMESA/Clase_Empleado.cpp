#include<cstring>
#include<cstdlib>
#include <iostream>
/// acordarse de la funcion cargar cadena para los char.
/// funcion menu
/*
void cargar_cadena(char *palabra, int tam){
    int i = 0;
    fflush(stdin);
    for (i = 0 ; i < tam ; i++ ){
        palabra[i] = cin.get();
        if(palabra[i] == '\n') break;
    }
    palabra[i] = '\0';
    fflush(stdin);
}
 */
using namespace std;

int main() {

   return 0;
}#include "Clase_Empleado.h"

Clase_Empleado::Clase_Empleado()
{
    //ctor
}
